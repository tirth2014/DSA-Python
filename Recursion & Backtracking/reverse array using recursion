arr = [5, 3, 7, 9, 1, 2, 4]


def reverse_arr(arr, i, n):
    if i >= n // 2: return
    arr[i], arr[n - i - 1] = arr[n - i - 1], arr[i]
    reverse_arr(arr, i + 1, n)
    
reverse_arr(arr, 0, len(arr))
print(arr)
